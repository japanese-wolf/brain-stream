---
description: "技術記事を収集・要約・分類し、階層的ダイジェストを生成する"
allowed-tools: [WebFetch, WebSearch, Read, Write, Bash, Task]
---

# BrainStream Digest

ユーザーの関心プロファイルに基づき、技術記事を収集・要約・分類し、階層的マークダウンダイジェストを生成する。

## 重要な制約

- **事実の正確性**: 原文にない情報を追加しない。装飾・誇張・推測を排除する。
- **ソース必須**: 全記事にソース URL を必ず添付する。URL のない情報は除外する。
- **出力言語**: ダイジェストは日本語で出力する。
- **プロファイル必須**: `profile.json` がない場合は `/brainstream:setup` を案内して停止する。

## 実行プロトコル

### Step 1: 初期化

1. プロファイルを読み込む:
   ```
   Read: .claude/brainstream/profile.json
   ```
   **ファイルが存在しない場合**:
   - ユーザーに「プロファイルが未設定です。セットアップを開始します。」と表示する。
   - **自動的に `/brainstream:setup` の問診フローを実行する**（setup.md の Step 2〜4 と同等の処理を行う）。
   - 問診完了後、生成された profile.json を読み込んで Step 2 へ続行する。

2. ソースカタログを読み込む:
   ```
   Read: config/sources.json（プラグインインストールディレクトリから）
   ```
   プラグインパスで見つからない場合はカレントディレクトリの `config/sources.json` を試す。

3. データディレクトリを準備:
   ```
   Bash: mkdir -p .claude/brainstream/cache .claude/brainstream/digests
   ```

4. 実行タイムスタンプを生成: `YYYY-MM-DDTHH-MM-SS`（例: `2026-02-12T14-30-45`）。このタイムスタンプをファイル名に使用する。

5. キャッシュを確認: `$ARGUMENTS` が "cached" または "cache" の場合、`.claude/brainstream/cache/` 内の最新の JSON ファイルを探す。
   - 最新のキャッシュが存在する → そのキャッシュを読み込み Step 5（クラスタリング）にスキップ
   - キャッシュなし → Step 2 へ

### Step 2: 取得戦略の決定

プロファイルの `topics` を読み、各トピックの取得方法を決定する。

#### 2.1 カタログマッチング

各トピックについて、sources.json の各ソースの `topics` フィールドと**大文字小文字を無視して**照合する。

```
例: profile.topics = ["AWS", "Kubernetes", "Rust"]

AWS → カタログにマッチ（aws-whatsnew, aws-blog等）
Kubernetes → カタログにマッチなし → WebSearch で探索
Rust → カタログにマッチなし → WebSearch で探索
```

マッチしたソースの重複を排除する（複数トピックが同一ソースにマッチする場合）。

#### 2.2 取得タスクの構成

以下の2種類のタスクを構成する:

- **カタログ取得タスク**: マッチしたソースごとに1タスク。WebFetch で urls を順に試行。
- **探索タスク**: カタログにマッチしないトピックごとに1タスク。WebSearch で最新記事を探索。

### Step 3: 並列取得（サブエージェント）

**Task ツールでサブエージェントを並列起動し、全タスクを同時実行する。**

各サブエージェントには `model: "haiku"` を指定してコストと速度を最適化する。

#### 3.1 カタログ取得タスクのプロンプト（サブエージェント用）

各カタログ取得サブエージェントに以下を指示:

```
あなたは技術記事の収集エージェントです。以下のソースから記事を取得してください。

ソース情報:
- name: [ソース名]
- urls: [URL配列]
- type: [rss|scrape]
- source_class: [primary|secondary]
- domains: [ドメイン配列]

実行手順:
1. urls の最初の URL を WebFetch で取得する。
   - RSSソース: 「このRSSフィードから全記事エントリを抽出してください。各エントリの title, link(URL), published date, description を返してください。」
   - Scrapeソース: 「このページからリリースノート/更新情報を抽出してください。各エントリの title, date, description を返してください。」

2. WebFetch がリダイレクトを返した場合 → リダイレクト先 URL で再取得する（最大2回）。

3. **JS レンダリング失敗の検出**: WebFetch の結果が以下に該当する場合、そのページは JavaScript レンダリングが必要で WebFetch では取得不可と判断する:
   - レスポンスがほぼ空、または意味のあるコンテンツがない
   - "JavaScript is required", "Enable JavaScript", "Loading..." のみが返される
   - RSS/XML の構造が全くない（RSS ソースの場合）
   → この場合も「失敗」として扱い、次の URL に進む。

4. 最初の URL が失敗（エラー、空レスポンス、JS レンダリング不可）した場合 → urls の次の URL で再試行する。

5. **全 URL が失敗した場合 → WebSearch にフォールバックする。**
   - ソースの topics から適切な検索クエリを構成する（例: "[ソース名] latest news [年号]"）
   - WebSearch の結果から、ソースの domains に合致する記事を優先して抽出する
   - WebSearch でも記事が見つからない場合 → エラーを報告して終了:
     {"status": "error", "source": "[ソース名]", "error": "[エラー内容]", "tried": ["url1", "url2", "websearch"]}

5. 取得成功した場合、各記事を以下の形式で要約する:
   - **What**: 何が発表/変更されたか（1-2文、事実のみ）
   - **Who**: どのベンダー/組織か
   - **Why it matters**: エンジニアにとっての意義（1文、記事内容に基づく）

6. 最終出力は以下の JSON 形式:
   {
     "status": "success",
     "source_id": "[ソースID]",
     "source_name": "[ソース名]",
     "source_class": "[primary|secondary]",
     "domains": [ドメイン配列],
     "fetch_method": "catalog",
     "fetched_url": "[実際に取得したURL]",
     "articles": [
       {
         "title": "...",
         "url": "...",
         "date": "...",
         "summary": {
           "what": "...",
           "who": "...",
           "why_it_matters": "..."
         }
       }
     ]
   }

制約:
- 原文にない情報を追加しない
- 装飾的・誇張的な表現を使わない
- URL のない記事は除外する
- 最近1-2日以内の記事を優先する（日付が分かる場合）
```

#### 3.2 探索タスクのプロンプト（サブエージェント用）

各探索サブエージェントに以下を指示:

```
あなたは技術トレンドの探索エージェントです。以下のトピックに関する最新の技術記事を探索してください。

トピック: [トピック名]

実行手順:
1. WebSearch で「[トピック名] release news announcement [今年の年号]」を検索する。

2. 検索結果から、以下の基準で最大5件の記事を選択する:
   - 直近1週間以内の記事を優先
   - 公式発表・リリースノートを優先
   - ニュースサイトのまとめ記事は含めてよいが二次情報として扱う

3. 各記事を以下の形式で要約する:
   - **What**: 何が発表/変更されたか（1-2文、事実のみ）
   - **Who**: どのベンダー/組織か
   - **Why it matters**: エンジニアにとっての意義（1文）

4. 一次情報/二次情報の判定:
   - 公式ドメイン（ベンダー公式サイト）からの記事 → primary
   - ニュースサイト、ブログ等 → secondary

5. 最終出力は以下の JSON 形式:
   {
     "status": "success",
     "topic": "[トピック名]",
     "fetch_method": "websearch",
     "articles": [
       {
         "title": "...",
         "url": "...",
         "date": "...",
         "source_class": "primary|secondary",
         "summary": {
           "what": "...",
           "who": "...",
           "why_it_matters": "..."
         }
       }
     ]
   }

記事が見つからない場合:
   {"status": "no_results", "topic": "[トピック名]"}

制約:
- 原文にない情報を追加しない
- 装飾的・誇張的な表現を使わない
- URL のない記事は除外する
```

#### 3.3 並列実行

全サブエージェントを **1つのメッセージ内で複数の Task ツール呼び出し** として同時に起動する。
全エージェントの結果が返ってきたら Step 4 へ進む。

### Step 4: 集約と重複排除

全サブエージェントの結果を集約し、ノイズを除去する。

#### 4.1 結果の集約

- `status: "success"` の結果から全記事を収集する
- `status: "error"` の結果は失敗ソースとして記録する
- `status: "no_results"` の結果は「該当記事なし」として記録する

**記事がゼロ件の場合**: 全ソースの状況を報告して停止する。

#### 4.2 重複排除

**A. URL ベース重複排除（過去キャッシュとの比較）:**

`.claude/brainstream/cache/` 内の直近 7 日分のキャッシュ JSON を読み込み、既出 URL を収集する。
今回取得した記事の URL が既出 URL に完全一致する場合は除外する。

```
Bash: ls -t .claude/brainstream/cache/*.json 2>/dev/null | head -7
```

各キャッシュファイルから `articles[].url` を抽出し、既出 URL セットを構築する。

**B. 内容ベース重複排除（LLM 判定）:**

URL が異なるが同一の発表/ニュースを扱う記事を検出する。

全記事のタイトルと要約を一覧し、以下の基準で重複グループを特定する:
- 同じ製品/サービスの同じ発表を異なるソースが報道している
- タイトルや What の内容が実質的に同一

重複グループが見つかった場合:
- **一次情報（🏷️）を優先** して保持する
- 一次情報がない場合は最も詳細な記事を保持する
- 除外した記事は `deduplicated` としてキャッシュに記録（URL のみ保持）

#### 4.3 ソース分類の注釈

各記事に一次/二次の注釈を付与:

- **カタログ取得の記事**: ソースの `source_class` を使用。さらに記事 URL のドメインがソースの `domains` に含まれない場合は secondary に格下げ。
- **WebSearch 探索の記事**: サブエージェントの判定をそのまま使用。

#### 4.4 事実検証

各記事の要約を検証する:

1. **固有名詞・数値の一貫性**: 要約に含まれるバージョン番号、価格、日付が元記事の情報と矛盾していないか確認する。
2. **装飾の排除**: "革命的", "画期的", "ゲームチェンジャー" などの装飾的表現が混入していないか確認する。元記事が使っていない限り削除する。
3. **推測の排除**: "〜と予想される", "〜の可能性がある" など、元記事にない推測が追加されていないか確認する。

検証で問題が見つかった場合は要約を修正する。

### Step 5: クラスタリングとトレンド分析

#### 5.1 クラスタリング

全記事を読み、共通テーマでグループ化する:

1. 全記事の要約を俯瞰し、3-8 個の技術テーマを抽出する
2. 各記事を最も適切なテーマに割り当てる
3. テーマごとに 1-2 文のトレンド要約を生成する

**ルール:**
- テーマはベンダー名ではなく技術領域で命名する（例: "AI Infrastructure", "Developer Tools"）
- 2件未満のテーマは "Other" に統合する
- プロファイルの `granularity` が "overview" の場合は重要度の低い記事をスキップする
- プロファイルの `max_articles` が 0 以外の場合は、重要度順にその件数まで絞り込む

#### 5.2 新興トレンド検出

クラスタリング後、以下の分析を行う:

1. **急増トピック**: 過去のキャッシュ（直近 7 日）と比較し、今回のダイジェストで記事数が急増しているトピック/クラスタを検出する。
2. **新出トピック**: 過去のキャッシュに一度も登場していないクラスタ名やキーワードを検出する。

急増/新出トピックが見つかった場合、ダイジェストの概要に「🔥 新興トレンド」として含める。

#### 5.3 空白領域の探索

行動履歴（`.claude/brainstream/history/actions.json`）が存在し、`config.json` の `history_tracking` が `true` の場合:

1. 過去の explore 履歴で**深掘りされたことがないクラスタ/トピック**を特定する。
2. プロファイルの `domains` に含まれるが**最近の7日間で記事が少ない領域**を特定する。
3. これらの「空白領域」について、1 件の追加 WebSearch 探索を実行する:
   - WebSearch: "[空白トピック] latest developments [年号]"
   - 発見があればダイジェストの末尾に「💡 見落としチェック」セクションとして追加する。

行動履歴が無効/存在しない場合はこのステップをスキップする。

#### 5.4 パーソナライズ（行動履歴活用）

行動履歴が存在する場合、以下をクラスタリングと記事選択に反映する:

- **よく深掘りするトピック**: 関連記事をクラスタ内で上位に配置する
- **スキップされがちなトピック**: 重要度の閾値を引き上げる（重要なもののみ表示）

行動履歴が無効/存在しない場合はこのステップをスキップし、通常の重要度順で配置する。

#### 5.5 全体概要

ダイジェスト全体のサマリを 2-3 文で生成する。最も注目すべきトレンドを含める。
新興トレンドがあれば概要に含める。

### Step 6: ダイジェスト出力

以下の形式でユーザーに直接出力する:

```markdown
# Tech Digest — YYYY-MM-DD

## 概要
X件の記事を収集し、Yつのクラスタに分類しました。
[1-2文の注目トレンド]

## ソース取得状況
| トピック | 方法 | 状態 |
|---------|------|------|
| [トピック名] | カタログ / 探索 | 成功: N件 / 失敗: [理由] / 該当なし |

---

## [クラスタ名]
> [1-2文のトレンド要約]

### [記事タイトル] 🏷️

**What**: ...

**Who**: [ベンダー]

**Why it matters**: ...

🔗 [ソースURL]

### [記事タイトル] 📝

**What**: ...

**Who**: [ベンダー]

**Why it matters**: ...

🔗 [ソースURL]

---

## [クラスタ名]
> [トレンド要約]

### ...

---

## 🔥 新興トレンド
（Step 5.2 で検出された場合のみ表示）

| トピック | 種別 | 詳細 |
|---------|------|------|
| [トピック名] | 急増 / 新出 | [説明] |

---

## 💡 見落としチェック
（Step 5.3 で発見があった場合のみ表示）

> あなたの関心領域で最近カバーされていないトピックを探索しました。

### [記事タイトル]

**What**: ...

**Who**: ...

**Why it matters**: ...

🔗 [ソースURL]
```

**フォーマットルール:**
- 各記事の **What**, **Who**, **Why it matters**, 🔗 の間は**必ず空行**を入れる（詰まると読みづらいため）
- 記事タイトル（`###`）の後にも空行を入れる

**注釈:**
- 🏷️ = 一次情報（公式ベンダー発表）
- 📝 = 二次情報（ブログ、ニュース、第三者報道）

### Step 7: 保存

1. **キャッシュ保存** — `.claude/brainstream/cache/YYYY-MM-DDTHH-MM-SS.json` に Write（Step 1 で生成したタイムスタンプを使用）:

```json
{
  "timestamp": "YYYY-MM-DDTHH-MM-SS",
  "fetched_at": "ISO-8601 timestamp",
  "profile_topics": ["AWS", "Kubernetes", "Rust"],
  "fetch_results": [
    {
      "topic_or_source": "...",
      "method": "catalog|websearch",
      "status": "success|error|no_results",
      "article_count": N
    }
  ],
  "articles": [
    {
      "title": "...",
      "url": "...",
      "date": "...",
      "source_id": "...",
      "source_class": "primary|secondary",
      "fetch_method": "catalog|websearch",
      "summary": {
        "what": "...",
        "who": "...",
        "why_it_matters": "..."
      },
      "cluster": "cluster-name"
    }
  ],
  "clusters": [
    {
      "name": "...",
      "article_count": N,
      "trend_summary": "..."
    }
  ],
  "deduplicated": [
    {
      "url": "...",
      "reason": "url_match|content_duplicate",
      "kept_url": "..."
    }
  ],
  "emerging_trends": [
    {
      "topic": "...",
      "type": "surge|new",
      "detail": "..."
    }
  ],
  "gap_exploration": [
    {
      "topic": "...",
      "articles_found": N
    }
  ]
}
```

2. **ダイジェスト保存** — `.claude/brainstream/digests/YYYY-MM-DDTHH-MM-SS.md` に Write（同じタイムスタンプを使用）

3. ユーザーに保存先を通知:
   - 「キャッシュ: `.claude/brainstream/cache/YYYY-MM-DDTHH-MM-SS.json`」
   - 「ダイジェスト: `.claude/brainstream/digests/YYYY-MM-DDTHH-MM-SS.md`」

## 引数

`$ARGUMENTS` の解釈:

- **"cached"** または **"cache"** — 今日のキャッシュデータを使用（再取得しない）
- **トピック名**（例: "AWS"） — そのトピックのみに絞って取得
- **指定なし** — プロファイルの全トピックで取得
